import React, { Component } from 'react';

import ServerBuilder from './containers/ServerBuilder/ServerBuilder';

class App extends Component {
  render() {
    return (
      <ServerBuilder />
    );
  }
}

export default App;
